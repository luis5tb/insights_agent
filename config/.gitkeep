# This directory contains configuration files for the Lightspeed Agent pod.
#
# Required files:
# - vertex-credentials.json (if using Vertex AI)
#
# Copy your credentials and configuration files here before running the pod.
